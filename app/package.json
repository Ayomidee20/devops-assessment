{
  "name": "devops-assessment-api",
  "version": "1.0.0",
  "private": true,
  "description": "Simple Node.js API for DevOps assessment (Express + Redis).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "jest --runInBand"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "redis": "^4.7.1"
  },
  "devDependencies": {
    "jest": "^30.1.3",
    "supertest": "^7.1.1"
  }
}
